$maybe msg <- mensagem
    <div class="alert alert-danger">
        #{msg}
<div.container>
    <h2>
        Consulta de Processo Por Autor
    <div.formulario>
        <form action=@{ConsultaPorAutorR} method=post enctype=#{enctype}>
            ^{widgetForm}
            <input type="submit" value="Pesquisar">